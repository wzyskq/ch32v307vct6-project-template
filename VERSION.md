# Version

## 规划

### 优化目标

- 按键优化
- 串口中断发送

### 版本映射

- [x] 1.0.0 - 完成基础功能；兼容 ZDT 步进电机

## 样例
- `*`：未测试版本
- `@`：已知问题版本
- `!`：无法编译/烧录版本
- `<`：更新历史功能版本
~~- `v`：可编译烧录版本~~
~~- `u`：无法编译或烧录版本~~

## 版本

### 1.0.0
- `Imp` 优化文件夹命名及结构
- `Imp` 统一驱动封装风格
- `New` 新增 types.h 头文件存放公共类型定义
- `New` 定时器初始化 pwmIn 模式
- `Fix` 中断 LTO 优化问题
- `Fix` 浮点格式化输出问题
- `Fix` math.h 编译链接配置问题

### v0.4.1
- `新增` oled 模块（江协改）、sys 文件

### v0.4.0
- `优化` 函数注释更新
- `修复` LED 超幅重启

### v0.3.1
- `优化` 定时器 pwm 初始化可附带多通道

## v0.3.0
- `新增` 定时器 pwm 输出
- `新增` servo 舵机文件

## v0.2.0
- `新增` funs 文件，用于存储自定义函数
- `优化` 串口命令解析

## v0.1.0
- `新增` 串口发送和回调功能
- `优化` LED 细节，增加切换功能
 
## t1.0
- `优化` 整理项目文件

## t0.4
- `优化` 完善 README 相关文件

## t0.3
- `新增` 板载 LED 示例
- `新增` LICENSE 

## t0.1
- `修复` 自定义无校验烧录脚本，避免 CRC 校验报错后等待 20s

## t0.0
- `新增` 模板文件，兼容 MS2 和 VSCode EIDE
